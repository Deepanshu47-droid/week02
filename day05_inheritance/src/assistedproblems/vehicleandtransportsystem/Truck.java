package assistedproblems.vehicleandtransportsystem;

public class Truck extends Vehicle {

    //constructor to initialize values
    public Truck(double maxSpeed, String fuelType) {
        super(maxSpeed, fuelType);
    }
}
